# Archived 404 source

The active custom 404 page is now served from:

```text
/404.html
```

This repository is configured with `.nojekyll`, so GitHub Pages serves static files directly and does not rely on this Markdown file to generate the error page.
